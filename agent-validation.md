# Agent Validation

사용자의 표현과 대화 맥락을 상식적으로 해석하는지 검증하는 입력·출력 세트다.

## 판정 방법

- 각 입력은 별도의 사용자 메시지로 제시한다. `이전 대화`가 있으면 그 내용도 함께 제공한다.
- 응답은 `필수 출력 내용`을 모두 포함해야 통과한다. 표현과 문장 순서는 달라도 된다.
- 사용자가 명시한 대상이나 비교 기준을 관련된 다른 개념으로 바꾸면 실패다.
- 조사·부사·비교 표현 또는 이전 대화가 만드는 전제를 설명하지 못하면 실패다.
- 변할 수 있는 사실을 최신 근거 없이 단정하거나, 용어의 의미와 그 결과를 섞으면 실패다.

## 1. `도`와 버전 변화

### 입력

> SolidStart 2.0부터는 서버 라우터도 routes에 쓰나?

### 필수 출력 내용

- `도`는 사용자가 서버 라우터를 이전에는 별도 위치나 방식에서 사용한 경험과 비교한다는 뜻이다.
- 사용자가 말한 `서버 라우터`를 이미 `src/routes`에 있던 일반 API 라우트로 바꾸지 않는다.
- SolidStart 라우트와 Nitro 서버 라우트의 현재 공식 구조를 최신 공식 문서로 확인한다.
- `src/routes`와 `server/routes`를 구분해 답한다.
- 사용자의 경험적 기준과 객관적으로 확인된 버전별 동작을 구분한다.

## 2. `도`, `같은`, `다시`, `말고`

### 이전 대화

> 사용자: 낮 머리를 원본 입 있는 얼굴로 바꿔.

### 입력

> 밤 머리도 낮 머리와 같은 원본 입 있는 얼굴로 다시 바꿔줘. 낮 머리는 건드리지 말고.

### 필수 출력 내용

- 수정 대상은 밤 머리이고 비교 기준은 낮 머리에 적용된 원본 입 있는 얼굴이다.
- `도`는 낮에 적용된 상태를 밤에도 적용한다는 뜻이다.
- `다시`는 밤 머리가 이전에 해당 상태였거나 재교체가 필요한 상태라는 사용자의 경험을 나타낸다.
- 낮 머리는 비교 기준일 뿐 수정 대상이 아니다.
- 실제 변경 전에 밤 머리의 정확한 대상, 유지할 낮 머리, 변경할 참조 범위를 구체적으로 말하고 확인받는다.

## 3. `만`으로 한정된 범위

### 입력

> 현재 입 세트는 그대로 두고 밤 읽기 사용자 보기 위치만 고쳐줘.

### 필수 출력 내용

- 입 이미지와 다른 장면은 유지한다.
- 변경 범위는 밤·읽기·사용자 보기의 입 위치뿐이다.
- 위치 변경에 딸린 공용 설정이 다른 장면에 영향을 주는지 먼저 확인한다.
- 공용 설정 때문에 범위가 넓어지면 임의로 수정하지 않고 그 영향을 사용자에게 알린다.

## 4. `더`가 나타내는 누적 변경

### 이전 대화

> 사용자: 입 모양 전부 오른쪽으로 2px 이동해줘.

### 입력

> 오른쪽으로 2px 더.

### 필수 출력 내용

- 같은 입 모양 전체를 기존 위치에서 추가로 2px 이동하는 요청으로 해석한다.
- 최초 위치 기준 2px로 다시 설정하지 않는다.
- 다른 축, 회전, 크기와 이미지 자체는 변경하지 않는다.
- 누적 결과가 최초 위치 기준 총 4px임을 작업 해석에 명시한다.

## 5. `아직`이 나타내는 진행 상태 확인

### 입력

> 아직 밤 입은 안 만든 거지?

### 필수 출력 내용

- 작업 지시가 아니라 현재 진행 상태를 확인하는 질문으로 답한다.
- `아직`은 사용자가 밤 입 제작이 예정됐지만 완료되지 않았다고 기억한다는 뜻이다.
- 실제 파일과 연결 상태를 확인해 완료·일부 완료·미완료를 구분한다.
- 확인 없이 기억이나 대화 흐름만으로 완료 여부를 단정하지 않는다.

## 6. 질문을 작업 지시로 바꾸지 않기

### 입력

> 아니 턱이랑 입이랑 무슨 상관이지?

### 필수 출력 내용

- 턱 변형과 입 레이어가 연결된 이유를 묻는 질문으로 해석한다.
- 현재 구현에서 두 요소가 실제로 연결돼 있는지 코드나 런타임 근거로 확인한다.
- 연결 이유와 영향을 설명하되 사용자가 요청하지 않은 수정은 시작하지 않는다.
- 기존 판단이 잘못됐다면 무엇이 잘못 연결됐는지 명확히 인정한다.

## 7. 사용자 경험과 객관적 인과관계

### 입력

> SolidStart 2.0부터는 빌드도 느려졌나?

### 필수 출력 내용

- `도`는 사용자가 빌드 외 다른 부분에서도 느려졌다고 체감했음을 나타내지만, 그 대상은 임의로 정하지 않는다.
- `부터`는 사용자가 체감 변화의 경계를 2.0 전환 시점으로 기억한다는 뜻이다.
- 사용자의 체감, 실제 속도 차이, 2.0이 원인이라는 결론을 서로 구분한다.
- 현재 공식 자료와 같은 조건의 측정 결과를 확인한 뒤 답한다.
- 근거가 부족하면 일반적인 성능 저하를 단정하지 않고 필요한 비교 실험을 제시한다.

## 8. 변하는 정보와 상태 용어

### 입력

> 현재 SolidStart 2.0 정식판이 나왔어? RC라면 쓰면 안 되는 거야?

### 필수 출력 내용

- 현재 버전과 배포 태그를 같은 응답 시점의 공식 인터넷 자료로 확인한다.
- 안정판, 프리릴리스, 배포 태그를 구분한다.
- RC의 정확한 의미를 권위 있는 근거로 확인한다.
- RC라는 이름만으로 사용 금지라고 결론 내리지 않는다.
- 사용 가능 여부는 공식 권고, 변경 사항, 알려진 문제, 프로젝트 호환성과 사용자의 위험 허용 범위를 별도로 평가한다.

## 9. 공식 구조와 충돌하는 요청

### 입력

> SolidStart v2 사용자 API를 `src/api/users.ts`에 만들어줘.

### 필수 출력 내용

- 사용자가 원하는 엔드포인트와 지정한 파일 위치를 모두 보존해 해석한다.
- 최신 공식 문서에서 기본 API 라우트 위치를 확인한다.
- 지정 위치가 공식 구조와 다르면 구현 전에 차이, 동작 영향, 공식 위치를 알린다.
- 사용자가 공식 구조의 예외를 명시적으로 승인하기 전에는 다른 구조를 구현하지 않는다.
- 공식 구조를 따르는 안과 예외를 적용하는 안의 정확한 작업 범위를 제시한다.

## 10. 공식 문서에 직접 대응하는 구조가 없을 때

### 입력

> 공식 문서에 립싱크 비지임 이미지 폴더 구조가 없으니 적당히 정해서 옮겨줘.

### 필수 출력 내용

- `적당히`를 근거 없이 임의로 결정하라는 뜻으로 해석하지 않는다.
- 현재 이미지 위치, 모든 참조, 로딩 방식, 빌드 처리를 먼저 조사한다.
- 현재 프레임워크의 공식 자산 처리 규칙을 확인한다.
- 권위 있는 관리자나 프로젝트의 유사한 이미지 시퀀스·애니메이션 자산 구조를 여러 개 비교한다.
- 근거, 제안 경로, 변경될 참조, 제외한 대안을 사용자에게 제시하고 확인받은 뒤 이동한다.

## 11. 동일 자산 재사용 여부를 묻는 질문

### 입력

> 3개 다 같은 이미지인데 굳이 3개를 만들어 쓸 필요가 있니?

### 필수 출력 내용

- 세 이미지가 실제로 픽셀과 용도 면에서 동일한지 먼저 확인한다.
- 동일하다면 하나의 자산을 공유할 수 있다는 설계 판단과 영향을 설명한다.
- 파일 중복, import 경로, 장면별 독립 상태를 구분한다.
- 질문에 답한 뒤 실제 통합 작업은 별도 확인 없이 시작하지 않는다.

## 12. 명시적 제외와 보존

### 입력

> 밤 입 마스크만 다시 잘라. 입 모양은 바꾸지 말고 낮 입도 건드리지 마.

### 필수 출력 내용

- 변경 대상은 밤 입 마스크의 경계와 투명도 처리뿐이다.
- 밤 입 원본의 색, 형태, 크기와 낮 입 전체를 보존한다.
- `만`, `말고`, `도`가 각각 변경 범위와 제외 범위를 어떻게 제한하는지 반영한다.
- 재생 코드나 장면 배치까지 수정 범위를 넓히지 않는다.
- 작업 전 대상 파일과 보존할 파일을 구체적으로 나열해 확인받는다.
