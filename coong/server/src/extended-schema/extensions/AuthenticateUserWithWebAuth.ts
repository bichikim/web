import {
  // BaseSchemaMeta,
  Extension,
} from '@graphql-ts/extend'
// import {graphql} from '@graphql-ts/schema'
// base: BaseSchemaMeta,
export const authenticateUserWithWebAuth = (): Extension => {
  return {
    //
  }
}
