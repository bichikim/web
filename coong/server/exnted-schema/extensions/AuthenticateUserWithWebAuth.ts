
import {BaseSchemaMeta, Extension} from '@graphql-ts/extend'
// import {graphql} from '@graphql-ts/schema'

export const authenticateUserWithWebAuth = (base: BaseSchemaMeta): Extension => {
  return {
    //
  }
}
