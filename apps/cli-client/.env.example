# 개발 서버 프록시 대상 (vite.config.ts의 proxy에 사용). 기본 3040 (cli-server와 동일)
VITE_CLI_SERVER_PORT=3040

# POST 요청 URL. 개발 시 기본 `/agent` — Vite가 cli-server로 프록시함.
# 배포 후 다른 오리진의 cli-server를 직접 호출할 때만 전체 URL로 지정 (서버에서 CORS 필요).
# VITE_AGENT_POST_URL=http://localhost:3040/agent
