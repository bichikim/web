import {describe, expect, it} from 'vitest'

describe('auth end-point', () => {
  describe('sign-in', () => {
    it('should response User and token', () => {
      expect('foo').toBe('foo')
    })
  })
})
