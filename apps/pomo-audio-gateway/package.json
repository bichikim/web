{
  "name": "@apps/pomo-audio-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --env= --dry-run --outdir dist",
    "deploy": "wrangler deploy --env=",
    "dev": "wrangler dev",
    "lint": "oxlint src",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@pomo/playback-token": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260822.1",
    "typescript": "catalog:",
    "wrangler": "^4.125.0"
  }
}
