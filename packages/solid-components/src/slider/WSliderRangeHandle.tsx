export interface WSliderRangeHandleProps {
  /**
   * max value |(hear to)-------(hear)|
   */
  size?: number
}

/**
 * Handles with a range of sizes
 * @param props
 * @constructor
 */
export const WSliderRangeHandle = (props: WSliderRangeHandleProps) => {
  //
}
