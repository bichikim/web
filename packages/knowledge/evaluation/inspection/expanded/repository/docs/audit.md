---
knowledge:
  id: refresh/audit
  type: rule
---

# Token refresh audit {#policy}

Every token refresh request must produce an audit log entry after it completes. This requirement does not specify whether refresh requests run sequentially or concurrently.
