{
  "name": "@winter-love/functions",
  "private": true,
  "version": "1.0.1-alpha.0",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "yarn run build && concurrently \"yarn run build -w --assumeChangesOnlyAffectDirectDependencies\" \"firebase emulators:start --only functions\"",
    "dev-all": "yarn run dev",
    "build": "tsc",
    "lint": "eslint --ext .ts \"src/**/*.{ts,tsx}\"",
    "deploy": "firebase deploy --only functions",
    "init": "npm install",
    "logs": "firebase functions:log",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "test": "mocha"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1",
    "register-service-worker": "^1.7.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "chai": "^4.2.0",
    "eslint": "^7.6.0",
    "eslint-plugin-import": "^2.22.0",
    "firebase-functions-test": "^0.2.0",
    "mocha": "^8.1.3",
    "node-mocks-http": "^1.9.0",
    "typescript": "^4.0.3",
    "ts-node": "^9.0.0"
  },
  "engines": {
    "node": "12"
  }
}
